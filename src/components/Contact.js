import React from 'react'

const Contact = (props) => {
    return (
        <div>
            <input placeholder="Name"/>
            <textarea placeholder="message"/>
            <button>send</button>
        </div>
    )
}

// REFERENCE INPUTS IN WEATHER APP OR INCREMENTOR CLASS LESSON

export default Contact